@model IEnumerable<LHDBaiTapLon214.Models.ThongTin>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm Mới", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenSinhVien)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NgayThangNam)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenSinhVien)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NgayThangNam)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Edit", new { id=item.MaSinhVien }) |
            @Html.ActionLink("Chi Tiết", "Details", new { id=item.MaSinhVien }) |
            @Html.ActionLink("Xóa", "Delete", new { id=item.MaSinhVien })
        </td>
    </tr>
}

</table>
