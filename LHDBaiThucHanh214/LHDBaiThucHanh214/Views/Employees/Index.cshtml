@model IEnumerable<LHDBaiThucHanh214.Models.Employee>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Thêm mới", "Thêm")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeName)
        </td>
        <td>
            @Html.ActionLink("Sửa", "Sửa", new { id=item.EmployeeID }) |
            @Html.ActionLink("Chi Tiết", "Chi Tiết", new { id=item.EmployeeID }) |
            @Html.ActionLink("Xóa", "Xóa", new { id=item.EmployeeID })
        </td>
    </tr>
}

</table>
